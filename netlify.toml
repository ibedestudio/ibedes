[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

[functions]
  node_bundler = "esbuild"

# Environment variables (uncomment and set if needed)
# [build.environment]
#   NODE_VERSION = "18"
